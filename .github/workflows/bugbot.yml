name: Bugbot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  configured:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f .cursor/BUGBOT.md
