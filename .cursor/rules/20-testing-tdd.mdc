---
description: TDD enforcement - failing test before any implementation
globs: src/**/*.ts, src/**/*.tsx, apps/**/*.ts, apps/**/*.tsx, packages/**/*.ts, tests/**/*
alwaysApply: false
---
# Test-Driven Development

Implementation code was removed in the 2026-05-27 reset. This rule is inactive
until development resumes. Future testing/TDD gates must be re-ratified in the
vault before implementation work.
